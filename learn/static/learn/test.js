function test() {
	//返回的是URL 是要被URL.py解析的 不是文件存储的路径
	window.location = "playing";
}

function generate_sequences(){
	
}

function redirect(sequence,current_page){
	// jump to the next page according to current_page and given sequence 
	
}

function save(){
	
}